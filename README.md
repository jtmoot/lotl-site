# lotl-site

LOTL website.

## Setup

_TBD_

## Secrets

Secrets are managed via Doppler — never commit them. `.env` is local-only and gitignored; the committed contract is `.env.example` with placeholders only.

Run with:

```bash
doppler run -- <command>
```
